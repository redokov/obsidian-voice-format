{
  "name": "obsidian-voice-format",
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild main.ts --bundle --outfile=main.js --format=cjs --platform=node --external:obsidian --minify"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.0",
    "@types/node": "^20.0.0"
  }
}
